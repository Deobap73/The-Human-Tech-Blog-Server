# Railway configuration for the server service only
[services]
  [services.server]
    # Path to the server Dockerfile
    dockerfilePath = "The-Human-Tech-Blog-Server/Dockerfile"
    # Port your Express app listens on
    port = 5000